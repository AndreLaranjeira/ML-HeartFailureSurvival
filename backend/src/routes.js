// Package imports.
const express = require("express");

// Controller imports.

// Validator imports.

// Router.
const routes = express.Router();

// Something routes.

// Export module.
module.exports = routes;
