// Package imports.
import React from "react";
import ReactDOM from "react-dom";

// Component imports.
import App from "./App";

// Rendering.
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
